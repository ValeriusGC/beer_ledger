/// Маркер value object в DDD: значение без собственной идентичности.
///
/// Пустой контракт роли. Реальные VO (`AxisContribution` и enum вроде `AxisSign`)
/// лежат в папке своего агрегата. Маркер — в ядре: это механизм, не язык порции
/// или журнала.
abstract class ValueObject {}
