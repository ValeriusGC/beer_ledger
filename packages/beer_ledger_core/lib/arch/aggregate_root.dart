/// Маркер корня агрегата в DDD.
///
/// Пустой контракт роли: инварианты и идентичность живут в конкретном типе
/// (`Clicker`, `Click`), не здесь. Лежит в техническом ядре, а не в контексте:
/// оба языка помечают свои корни одним маркером, без зависимости друг от друга.
abstract class AggregateRoot {}
