name: beer_ledger_core
description: Pure Dart domain — clicker, ledgers, aggregation, unit conversion (Пивомер / Beer Ledger).
version: 0.0.1
publish_to: none

environment:
  sdk: ^3.12.2

dependencies:
  fpdart: ^1.2.0
  freezed_annotation: ^3.1.0

dev_dependencies:
  build_runner: ^2.13.0
  freezed: ^3.2.5
  lints: ^6.0.0
  test: ^1.26.3
